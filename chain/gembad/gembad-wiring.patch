diff --git a/evmd/app.go b/evmd/app.go
index 204345c..c081d7f 100644
--- a/evmd/app.go
+++ b/evmd/app.go
@@ -35,6 +35,22 @@ import (
 	"github.com/cosmos/evm/x/feemarket"
 	feemarketkeeper "github.com/cosmos/evm/x/feemarket/keeper"
 	feemarkettypes "github.com/cosmos/evm/x/feemarket/types"
+
+	// GembaBlockchain custom modules (Phase 2) — EVM-independent, isolated so
+	// upstream cosmos/evm bumps stay clean (CLAUDE.md §16.6).
+	feesplit "github.com/ivanovslavy/GembaBlockchain/chain/x/feesplit"
+	feesplitkeeper "github.com/ivanovslavy/GembaBlockchain/chain/x/feesplit/keeper"
+	feesplittypes "github.com/ivanovslavy/GembaBlockchain/chain/x/feesplit/types"
+	rewardstreamer "github.com/ivanovslavy/GembaBlockchain/chain/x/rewardstreamer"
+	rewardstreamerkeeper "github.com/ivanovslavy/GembaBlockchain/chain/x/rewardstreamer/keeper"
+	rewardstreamertypes "github.com/ivanovslavy/GembaBlockchain/chain/x/rewardstreamer/types"
+	tailreward "github.com/ivanovslavy/GembaBlockchain/chain/x/tailreward"
+	tailrewardkeeper "github.com/ivanovslavy/GembaBlockchain/chain/x/tailreward/keeper"
+	tailrewardtypes "github.com/ivanovslavy/GembaBlockchain/chain/x/tailreward/types"
+	valgate "github.com/ivanovslavy/GembaBlockchain/chain/x/valgate"
+	valgatekeeper "github.com/ivanovslavy/GembaBlockchain/chain/x/valgate/keeper"
+	valgatetypes "github.com/ivanovslavy/GembaBlockchain/chain/x/valgate/types"
+	slashfunds "github.com/ivanovslavy/GembaBlockchain/chain/x/slashfunds"
+
 	ibccallbackskeeper "github.com/cosmos/evm/x/ibc/callbacks/keeper"
 	"github.com/cosmos/evm/x/vm"
 	evmkeeper "github.com/cosmos/evm/x/vm/keeper"
@@ -186,6 +202,12 @@ type EVMD struct {
 	Erc20Keeper     erc20keeper.Keeper
 	EVMMempool      sdkmempool.ExtMempool
 
+	// GembaBlockchain custom keepers (Phase 2)
+	RewardStreamerKeeper rewardstreamerkeeper.Keeper
+	TailRewardKeeper     tailrewardkeeper.Keeper
+	FeeSplitKeeper       feesplitkeeper.Keeper
+	ValgateKeeper valgatekeeper.Keeper
+
 	// the module manager
 	ModuleManager      *module.Manager
 	BasicModuleManager module.BasicManager
@@ -241,6 +263,9 @@ func NewExampleApp(
 		ibcexported.StoreKey, ibctransfertypes.StoreKey,
 		// Cosmos EVM store keys
 		evmtypes.StoreKey, feemarkettypes.StoreKey, erc20types.StoreKey,
+		// GembaBlockchain custom module store keys
+		rewardstreamertypes.StoreKey, feesplittypes.StoreKey,
+		tailrewardtypes.StoreKey, valgatetypes.StoreKey,
 	)
 	oKeys := storetypes.NewObjectStoreKeys(banktypes.ObjectStoreKey, evmtypes.ObjectKey)
 
@@ -309,6 +334,14 @@ func NewExampleApp(
 	)
 	app.BankKeeper = app.BankKeeper.WithObjStoreKey(oKeys[banktypes.ObjectStoreKey])
 
+	// GembaBlockchain custom keepers (Phase 2). The SDK bank keeper satisfies their
+	// BankKeeper interfaces, which omit Mint/Burn — so neither module can change
+	// total supply (zero inflation §3.1). See chain/x/WIRING.md.
+	app.RewardStreamerKeeper = rewardstreamerkeeper.NewKeeper(keys[rewardstreamertypes.StoreKey], app.BankKeeper)
+	app.TailRewardKeeper = tailrewardkeeper.NewKeeper(keys[tailrewardtypes.StoreKey], app.BankKeeper)
+	app.FeeSplitKeeper = feesplitkeeper.NewKeeper(keys[feesplittypes.StoreKey], app.BankKeeper)
+	app.ValgateKeeper = valgatekeeper.NewKeeper(appCodec, keys[valgatetypes.StoreKey], authAddr)
+
 	// optional: enable sign mode textual by overwriting the default tx config (after setting the bank keeper)
 	enabledSignModes := append(authtx.DefaultSignModes, signingtypes.SignMode_SIGN_MODE_TEXTUAL) //nolint:gocritic
 	txConfigOpts := authtx.ConfigOptions{
@@ -327,9 +360,14 @@ func NewExampleApp(
 	app.StakingKeeper = stakingkeeper.NewKeeper(
 		appCodec,
 		runtime.NewKVStoreService(keys[stakingtypes.StoreKey]),
 		app.AccountKeeper,
-		app.BankKeeper,
+		// GembaBlockchain: redirect slashed stake to the faucet reserve instead of
+		// burning it, so total supply stays fixed at 100M (CLAUDE.md §3.1, §5.6;
+		// ADR-013). The wrapper intercepts ONLY BurnCoins from the staking bonded /
+		// not-bonded pools (= slashing) and SendCoinsFromModuleToModule it to the
+		// faucet; every other bank call passes through. Zero-burn, like x/feesplit.
+		slashfunds.NewBankKeeper(app.BankKeeper, feesplittypes.DefaultFaucetAccount),
 		authAddr,
 		evmaddress.NewEvmCodec(sdk.GetConfig().GetBech32ValidatorAddrPrefix()),
 		evmaddress.NewEvmCodec(sdk.GetConfig().GetBech32ConsensusAddrPrefix()),
 	)
@@ -365,7 +398,10 @@ func NewExampleApp(
 	app.FeeGrantKeeper = feegrantkeeper.NewKeeper(appCodec, runtime.NewKVStoreService(keys[feegrant.StoreKey]), app.AccountKeeper)
 
+	// valgate: inject the staking keeper (read-only) for the AfterValidatorCreated floor hook,
+	// enforcing the §5.2 min-self-bond on BOTH the Cosmos and EVM-precompile paths (audit #1).
+	app.ValgateKeeper = app.ValgateKeeper.WithStakingKeeper(app.StakingKeeper)
+
 	// register the staking hooks
 	// NOTE: stakingKeeper above is passed by reference, so that it will contain these hooks
 	app.StakingKeeper.SetHooks(
-		stakingtypes.NewMultiStakingHooks(app.DistrKeeper.Hooks(), app.SlashingKeeper.Hooks()),
+		stakingtypes.NewMultiStakingHooks(app.DistrKeeper.Hooks(), app.SlashingKeeper.Hooks(), app.ValgateKeeper.Hooks()),
 	)
@@ -583,6 +616,11 @@ func NewExampleApp(
 		vmModule,
 		feemarket.NewAppModule(app.FeeMarketKeeper),
 		erc20.NewAppModule(app.Erc20Keeper, app.AccountKeeper),
+		// GembaBlockchain custom modules (Phase 2)
+		feesplit.NewAppModule(app.FeeSplitKeeper),
+		rewardstreamer.NewAppModule(app.RewardStreamerKeeper),
+		tailreward.NewAppModule(app.TailRewardKeeper),
+		valgate.NewAppModule(app.ValgateKeeper),
 	)
 
 	// BasicModuleManager defines the module BasicManager which is in charge of setting up basic,
@@ -624,6 +662,11 @@ func NewExampleApp(
 		erc20types.ModuleName, feemarkettypes.ModuleName,
 		evmtypes.ModuleName, // NOTE: EVM BeginBlocker must come after FeeMarket BeginBlocker
 
+		// GembaBlockchain: split fees (feesplit), then stream the validator reward
+		// (rewardstreamer), then let distribution pay it out — so only fees are
+		// split and the reward lands before distribution (CLAUDE.md §5.4).
+		feesplittypes.ModuleName, rewardstreamertypes.ModuleName, tailrewardtypes.ModuleName,
+
 		// TODO: remove no-ops? check if all are no-ops before removing
 		distrtypes.ModuleName, slashingtypes.ModuleName,
 		evidencetypes.ModuleName, stakingtypes.ModuleName,
@@ -673,6 +716,9 @@ func NewExampleApp(
 		ibctransfertypes.ModuleName,
 		genutiltypes.ModuleName, evidencetypes.ModuleName, authz.ModuleName,
 		feegrant.ModuleName, upgradetypes.ModuleName, vestingtypes.ModuleName,
+		// GembaBlockchain custom modules
+		feesplittypes.ModuleName, rewardstreamertypes.ModuleName, tailrewardtypes.ModuleName,
+		valgatetypes.ModuleName,
 	}
 	app.ModuleManager.SetOrderInitGenesis(genesisModuleOrder...)
 	app.ModuleManager.SetOrderExportGenesis(genesisModuleOrder...)
@@ -812,7 +858,13 @@ func (app *EVMD) setAnteHandler(txConfig client.TxConfig, maxGasWanted uint64) {
 		panic(err)
 	}
 
-	app.SetAnteHandler(evmante.NewAnteHandler(options))
+	{
+		baseAnte := evmante.NewAnteHandler(options)
+		minBondDec := valgate.NewMinSelfBondDecorator(app.ValgateKeeper)
+		app.SetAnteHandler(func(ctx sdk.Context, tx sdk.Tx, sim bool) (sdk.Context, error) {
+			return minBondDec.AnteHandle(ctx, tx, sim, baseAnte)
+		})
+	}
 }
 
 func (app *EVMD) onPendingTx(hash common.Hash) {
diff --git a/evmd/config/permissions.go b/evmd/config/permissions.go
index 312b521..1fc0e3e 100644
--- a/evmd/config/permissions.go
+++ b/evmd/config/permissions.go
@@ -63,6 +63,14 @@ var maccPerms = map[string][]string{
 	vmtypes.ModuleName:        {authtypes.Minter, authtypes.Burner},
 	feemarkettypes.ModuleName: nil,
 	erc20types.ModuleName:     {authtypes.Minter, authtypes.Burner},
+
+	// GembaBlockchain custom modules (Phase 2) — NO Minter/Burner permissions:
+	// these accounts only hold and transfer pre-minted GMB, never change supply
+	// (zero inflation §3.1). String literals avoid importing the gemba modules here.
+	"rewardstreamer": nil, // holds the pre-minted 20M validator-reward reserve
+	"tailreward":     nil, // post-reserve recirculation buffer (ADR-008b)
+	"feesplit":       nil,
+	"faucet":         nil, // public/municipal reserve; receives 40% of fees (§5.4, §6)
 }
 
 // GetMaccPerms returns a copy of the module account permissions
