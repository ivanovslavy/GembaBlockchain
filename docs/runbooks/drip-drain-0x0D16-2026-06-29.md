# Drain dormant GembaDripFaucet (0x0D16) → founder — 2026-06-29

## Why
The faucet consolidation (2026-06-27) retired the old **governance drip faucet**; the single
system faucet is now **GembaFaucet `0x0147…`**. The dormant **GembaDripFaucet**
`0x0d16a7a490eb2f4766480424e28ee0187d5c74ab` still holds **10,000 GMB** — stranded in a
deprecated, Timelock-controlled contract. We recover it to the **founder/ops treasury**
`0x5578c75F22dE0bf1caA4BdD46BA28406C696a5dC`. The contract is governance-controlled (no
unilateral key, CLAUDE.md §3.6), so the recovery is done via a **Governor proposal**, not a
direct transfer.

## The proposal (CURRENT chain, post-regenesis — verified on-chain 2026-06-29)
- Governor `0xCCd9f78047E1BB8Bec419490E80409bfBf3B7b72`; proposal id
  `92090423469781120086472219325316130221223775591522426708624927065158942576673`; created block 14448.
- Target `0x0d16a7a490eb2f4766480424e28ee0187d5c74ab` (the dormant drip faucet, balance **10,000 GMB**).
- Action: release 10,000 GMB → founder `0x5578c75F…`.
- State (block 88,566): **Active** · votes **FOR 100 / AGAINST 0 / ABSTAIN 0** · quorum needed 40 → **passing**.
- Voting **deadline: block 100,849** (≈6.8 h after block 88,566) · Timelock `minDelay` = **300 s**.
- This is a **current-chain** proposal (NOT old-chain) — the regenesis was block 0; this is block 14448.

## Execute (only AFTER the voting deadline → state `Succeeded`)
1. `Governor.state(id)` must be `Succeeded` (4) — i.e. past block 100,849.
2. `Governor.queue(targets, values, calldatas, descriptionHash)` — params taken from the
   `ProposalCreated` event (target `0x0d16a7a4…`, value 0, the release calldata;
   `descriptionHash = keccak256(description)`). → state `Queued`.
3. Wait the **300 s** Timelock delay.
4. `Governor.execute(targets, values, calldatas, descriptionHash)` → 10,000 GMB → founder.
   → state `Executed`.

`queue`/`execute` are **permissionless** (anyone may call once `Succeeded`; only needs gas).
Full gov-flow recipe + the RPC-broadcast tunnel: `docs/runbooks/public-testnet-operations.md` §7.

## Status
- 2026-06-29: proposal Active & passing; **NOT yet executable** (voting open ~6.8 h more).
  Execute after the deadline. Until then the 10,000 GMB stay in `0x0D16` (safe — Timelock-locked).
